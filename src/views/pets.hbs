<h1>Lista de mascotas</h1>

<ul>
    {{#if pets.length}}
        {{#each pets}}
            <li>{{this.name}}</li>
        {{/each}}
    {{else}}
        <li>No hay mascotas disponibles</li>
    {{/if}}
</ul>